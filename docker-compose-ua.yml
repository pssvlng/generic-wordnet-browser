services:
  frontend_af:
    build:
      context: ./frontend
    labels:
      - traefik.enable=true
      - traefik.http.routers.frontend_ua.rule=PathPrefix(`/wordnet-ua`)
      - traefik.http.services.frontend_ua.loadbalancer.server.port=80
 
  backend_af:
    build:
      context: ./backend
    labels:
      - traefik.enable=true
      - traefik.http.routers.backend_ua.rule=PathPrefix(`/api/dict`) && Headers(`X-Lang`, `ua`)
      - traefik.http.services.backend_ua.loadbalancer.server.port=5000      
 